spring:
  servlet:
    multipart:
      max-file-size: 10MB
  jackson:
    serialization:
      FAIL_ON_EMPTY_BEANS: false
  application:
    name: salespage-service

server:
  port: ${PORT:8080}


feign:
  domain: ${SERVER_DOMAIN}